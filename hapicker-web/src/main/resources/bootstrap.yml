server:
  port: 8081
spring:
  application:
    name: hapicker-web
  cloud:
    config:  # git管理配置
      uri: http://127.0.0.1:6018
      label: master
      profile: dev
#      discovery:
#        enabled: true #是从配置中心读取文件。
#        service-id: hpWeb #配置中心的servieId，即服务名。




